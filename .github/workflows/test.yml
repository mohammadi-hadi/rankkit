name: Tests

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.14"]
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install
        run: pip install -e ".[dev]"
      - name: Lint
        run: ruff check .
      - name: Test
        run: pytest -q
      - name: CLI smoke test
        run: |
          rankkit --version
          rankkit eval examples/run_a.jsonl
          rankkit compare examples/run_a.jsonl examples/run_b.jsonl --json > /dev/null
          rankkit bias examples/clicks.jsonl examples/run_b.jsonl --json > /dev/null
      - name: Example fixtures are reproducible
        run: |
          python examples/make_fixtures.py
          git diff --exit-code -- examples/
